import { Component } from "@/components/ui/moving-circles-shader";

/**
 * Canonical demo from the integration prompt — the shader filling the viewport,
 * untouched. The console in App.tsx wraps this same Component with live chrome.
 */
export default function DemoOne() {
  return <Component />;
}
