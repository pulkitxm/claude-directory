import React from "react";
import ReactDOM from "react-dom/client";
import ThermalDemo from "./demo";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <ThermalDemo />
  </React.StrictMode>,
);
