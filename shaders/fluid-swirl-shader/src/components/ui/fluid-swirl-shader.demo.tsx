import { FluidSwirl } from "@/components/ui/fluid-swirl-shader";

/**
 * The canonical demo shipped with the component (verbatim from the integration
 * prompt): the full-viewport fluid swirl rendered with the original three
 * pigments, contrast 2.0, spin 0.36 and pixel_filter 700.
 */
export default function DemoOne() {
  return <FluidSwirl />;
}
