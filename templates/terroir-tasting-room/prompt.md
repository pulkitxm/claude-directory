# TERROIR — LUXURY BOUTIQUE WINE 'TASTING ROOM' LANDING PAGE

> THIS PROMPT.MD IS SELF-CONTAINED AND REPRODUCIBLE. THE TOP HALF IS THE VERBATIM
> ORIGINAL DESIGN BRIEF (UPPERCASED). THE BOTTOM HALF ("IMPLEMENTATION NOTES")
> CAPTURES EVERY COLOR TOKEN, FONT, TIMING, EASING, LAYOUT VALUE AND THE CORE
> CODE FOR THE SIGNATURE INTERACTIONS SO THAT FEEDING THIS FILE TO OPUS AGAIN
> PRODUCES THE SAME OUTPUT.
>
> SOURCE REFERENCE: A SUPERDESIGN DRAFT AT
> `https://p.superdesign.dev/draft/ee2df213-4905-4af3-b068-74ee0b4aa401`.
> NOTE: THAT HOST IS NOT ON THIS ENVIRONMENT'S NETWORK EGRESS ALLOWLIST, SO THE
> LIVE PAGE COULD NOT BE FETCHED/SCREENSHOTTED/FF-PROBED DURING THE BUILD. THE
> RECONSTRUCTION BELOW IS DERIVED FROM THE FULL WRITTEN SPEC, WHICH ALREADY
> ENUMERATES EVERY SECTION, COLOR, FONT, SIZE, TIMING AND THE SPECIAL-COMPONENT
> INTERNALS.

---

# SUMMARY

DESIGN A LUXURY BOUTIQUE LANDING PAGE WITH A 'TASTING ROOM' AESTHETIC. USE A
DARK-MODE PALETTE (#121212), EDITORIAL TYPOGRAPHY WITH DEEP KERNING, AND A
SOPHISTICATED GRAIN TEXTURE. THE LAYOUT SHOULD PRIORITIZE STORYTELLING THROUGH
GENEROUS WHITESPACE, HIGH-CONTRAST PHOTOGRAPHY, AND SUBTLE HOVER INTERACTIONS
THAT REVEAL PRODUCT DETAILS.

# STYLE

THE STYLE IS 'DARK MINIMALIST EDITORIAL.' IT USES A PALETTE OF #121212 (BLACK),
#EDEAE4 (CREAM), AND EARTHY ACCENTS LIKE #6B8E7F (SAGE) AND #8B5A3C (SIENNA).
TYPOGRAPHY PAIRS AN ELEGANT SERIF FOR HEADINGS WITH A MODERN SANS-SERIF FOR
FUNCTIONAL UI. VISUALS ARE UNIFIED BY A 5% OPACITY GRAIN OVERLAY AND
GRAYSCALE-TO-COLOR TRANSITIONS.

## SPEC

APPLY A BASE THEME OF #121212 BACKGROUND AND #EDEAE4 TEXT. IMPLEMENT A GLOBAL
GRAIN TEXTURE USING AN SVG FILTER (BASEFREQUENCY 0.65, 3 OCTAVES) AT 5% OPACITY
TO ADD TACTILE DEPTH. TYPOGRAPHY: HEADINGS USE 'CORMORANT GARAMOND'
(LIGHT/ITALIC, TRACKING-WIDER), UI/BODY USE 'MANROPE' (WEIGHT 200-400,
TRACKING-WIDEST FOR UPPERCASE LABELS). ACCENTS: SAGE (#6B8E7F), SIENNA
(#8B5A3C), AND BURGUNDY (#4A2C3E). MOTION: USE 800MS EASE-OUT TRANSITIONS FOR
SECTION REVEALS WITH A 20PX Y-AXIS TRANSLATE. BORDERS SHOULD BE MINIMAL, USING
WHITE AT 5% OR 10% OPACITY FOR HAIR-LINE SEPARATORS.

# LAYOUT & STRUCTURE

AN 8-SECTION VERTICAL SCROLL LAYOUT THAT BALANCES CONVERSION UTILITY WITH BRAND
STORYTELLING. INCLUDES A CENTERED-LOGO HEADER, ASYMMETRICAL HERO, INTERACTIVE
GRID, AND A SPLIT-SCREEN MEMBERSHIP COMPARISON.

## HEADER

CREATE A FIXED HEADER WITH A BLUR EFFECT (BACKDROP-BLUR-MD) AND A 1PX BOTTOM
BORDER (WHITE/5). PLACE A SERIF LOGO ('TERROIR') IN THE ABSOLUTE CENTER.
NAVIGATION SHOULD BE 4-5 UPPERCASE TRACKING-WIDEST LINKS IN 12PX MANROPE ON THE
LEFT. RIGHT SIDE INCLUDES 'ACCOUNT' AND A SHOPPING BAG ICON WITH A SIENNA
(#8B5A3C) DOT INDICATOR ON HOVER.

## EDITORIAL HERO

LARGE-SCALE TYPOGRAPHY SECTION. MAIN H1 SHOULD BE CORMORANT GARAMOND, 72PX-96PX,
WITH A LEADING OF 0.9. INCLUDE AN ITALICIZED SUB-PHRASE INDENTED BY 24PX. BELOW,
CREATE A 3-COLUMN GRID SEPARATED BY 1PX TOP BORDERS. COLUMN 1: SHORT PHILOSOPHY
TEXT. COLUMN 2/3: DESCRIPTIVE COPY AND A BOUNCING ARROW ICON (LUCIDE:ARROW-DOWN)
FOR SCROLL GUIDANCE.

## INTERACTIVE FLAVOR WALL

A 4-COLUMN GRID OF CARDS, EACH 600PX TALL. DEFAULT STATE: A SOLID COLOR BLOCK
(E.G., #2A1D21, #3A3528). HOVER STATE: THE COLOR FADES TO REVEAL A HIGH-CONTRAST
IMAGE (SCALE 105% TO 100%) WITH A MIX-BLEND-OVERLAY. TEXT INSIDE (NO. 01, FLAVOR
NAME, TASTING NOTES) SHOULD SLIDE 8PX HORIZONTALLY ON HOVER.

## PRODUCT SPOTLIGHT

A 50/50 SPLIT LAYOUT ON A #262626 BACKGROUND. LEFT: A 3:4 ASPECT RATIO IMAGE
WITH A 'STAFF PICK' BADGE (BLURRED BLACK BACKGROUND, WHITE BORDER). RIGHT:
DETAILED PRODUCT SPECS USING A 2X2 GRID FOR TASTING NOTES, METHOD, AND RITUAL.
CTA SHOULD BE A SOLID CREAM (#EDEAE4) BUTTON WITH BLACK TEXT AND UPPERCASE 12PX
FONT.

## CASE BUILDER UTILITY

A CONVERSION TOOL FEATURING A PROGRESS BAR (SAGE #6B8E7F) INDICATING CAPACITY
(E.G., 4/6). ITEMS ARE DISPLAYED IN A 3-COLUMN GRID OF CARDS WITH #FFFFFF/05
BACKGROUND. EACH CARD CONTAINS A 128PX WINE BOTTLE IMAGE (GRAYSCALE/OVERLAY) AND
CIRCULAR -/+ BUTTONS WITH 1PX BORDERS FOR QUANTITY CONTROL.

## MEMBERSHIP COMPARISON

A MINIMALIST TABLE DIVIDED BY A SINGLE VERTICAL 1PX LINE (WHITE/10). LEFT:
'GUEST' TIER WITH REDUCED OPACITY (60%). RIGHT: 'MEMBER' TIER WITH FULL OPACITY,
HIGHLIGHTED BY SIENNA TEXT ACCENTS. INCLUDE A SIMPLE 'JOIN THE CLUB' BUTTON WITH
A 1PX BORDER.

## HOW IT'S MADE GRID

A 4-COLUMN PROCESS GRID. EACH STEP FEATURES A NUMERIC LABEL (01-04) IN SAGE, A
GRAYSCALE PHOTOGRAPH THAT TURNS TO COLOR ON HOVER (700MS TRANSITION), AND A
SHORT, MATTER-OF-FACT PARAGRAPH IN 14PX MANROPE.

## FOOTER

DEEP FOOTER WITH 4 COLUMNS. COLUMN 1: SERIF LOGO AND ADDRESS. COLUMNS 2-3:
SIMPLE LINK LISTS. COLUMN 4: NEWSLETTER SIGNUP WITH A SINGLE BOTTOM-BORDER INPUT
(WHITE/20) AND A SIENNA 'SUBSCRIBE' TEXT LINK. BOTTOM BAR: SMALL 10PX COPYRIGHT
AND SOCIAL LINKS IN ALL-CAPS.

# SPECIAL COMPONENTS

## INTERACTIVE GRID CARD

A COLOR-BLOCK CARD THAT REVEALS AN IMAGE AND TEXT ON HOVER.

STRUCTURE: RELATIVE CONTAINER, OVERFLOW-HIDDEN. LAYER 1: COLOR BACKGROUND (E.G.,
#2A1D21). LAYER 2: IMAGE WITH OPACITY-0, TRANSITION-700MS, SCALE-105,
MIX-BLEND-OVERLAY. LAYER 3: FLEX-COLUMN PADDING-8. ON GROUP-HOVER: LAYER 2
BECOMES OPACITY-40 AND SCALE-100. TEXT ELEMENTS SHOULD USE 'TRANSITION-TRANSFORM
DURATION-500' TO SLIDE IN FROM THE LEFT.

## PROGRESS CAPACITY INDICATOR

A MINIMALIST BAR SHOWING ITEM SELECTION PROGRESS.

128PX WIDE, 4PX TALL CONTAINER WITH BACKGROUND WHITE/10. INNER BAR SHOULD BE
SAGE (#6B8E7F) WITH WIDTH CONTROLLED BY A PERCENTAGE. PAIR WITH A MONOSPACED
FONT (E.G., '4/6') FOR A TECHNICAL, PRECISE FEEL.

---

# IMPLEMENTATION NOTES (REPRODUCIBLE DETAILS)

## TECH & DELIVERY

- SINGLE STATIC PAGE: `index.html` + `styles.css` + `main.js`. NO BUILD STEP, NO
  FRAMEWORK — RUNS OFFLINE FROM ANY STATIC SERVER (`python3 -m http.server`).
- HAND-WRITTEN CSS (NOT TAILWIND), BUT AUTHORED AGAINST THE TAILWIND TOKENS THE
  BRIEF NAMES (TRACKING-WIDEST = `0.2em`, TRACKING-WIDER = `0.05em`,
  BACKDROP-BLUR-MD = `blur(12px)`, DURATION-500/700/800, PADDING-8 = `2rem`).
- ALL ASSETS VENDORED LOCALLY UNDER `assets/`: FONTS AS SELF-HOSTED WOFF2
  (`assets/fonts/fonts.css`), PHOTOGRAPHY AS LOCALLY-GENERATED GRAINY
  GRADIENT-MESH JPGS, AND WINE-BOTTLE PNGS (`assets/img/`). NO REMOTE HOTLINKS.
  THE IMAGE GENERATOR (`assets/gen-images.mjs`, sharp) IS COMMITTED FOR
  REPRODUCIBILITY — RE-RUN IT TO REGENERATE THE STILLS.

## DESIGN TOKENS (CSS CUSTOM PROPERTIES)

```css
:root{
  --black:#121212; --cream:#EDEAE4;
  --sage:#6B8E7F; --sienna:#8B5A3C; --burgundy:#4A2C3E;
  --panel:#262626;            /* product spotlight bg */
  --card-a:#2A1D21; --card-b:#3A3528; --card-c:#1E2622; --card-d:#33232A; /* flavor blocks */
  --hair-5:rgba(255,255,255,.05); --hair-10:rgba(255,255,255,.10);
  --hair-20:rgba(255,255,255,.20);
  --serif:"Cormorant Garamond",Georgia,serif;
  --sans:"Manrope",system-ui,sans-serif;
  --mono:"DM Mono","SFMono-Regular",ui-monospace,monospace;
}
body{background:var(--black);color:var(--cream);font-family:var(--sans);
  font-weight:300;-webkit-font-smoothing:antialiased;}
h1,h2,h3,.serif{font-family:var(--serif);font-weight:300;letter-spacing:.02em;}
.label{font-size:12px;letter-spacing:.2em;text-transform:uppercase;font-weight:400;}
```

## GLOBAL GRAIN OVERLAY (SVG FILTER, 5% OPACITY, FIXED, POINTER-EVENTS-NONE)

```html
<svg class="grain-svg"><filter id="grain">
  <feTurbulence type="fractalNoise" baseFrequency="0.65" numOctaves="3" stitchTiles="stitch"/>
  <feColorMatrix type="saturate" values="0"/></filter>
  <rect width="100%" height="100%" filter="url(#grain)"/></svg>
```
```css
.grain-svg{position:fixed;inset:0;width:100%;height:100%;opacity:.05;
  pointer-events:none;z-index:9999;mix-blend-mode:overlay;}
```

## SECTION REVEAL (800MS EASE-OUT, 20PX Y TRANSLATE, INTERSECTION OBSERVER)

```css
.reveal{opacity:0;transform:translateY(20px);
  transition:opacity .8s ease-out,transform .8s ease-out;}
.reveal.in{opacity:1;transform:none;}
```
```js
const io=new IntersectionObserver((es)=>es.forEach(e=>{
  if(e.isIntersecting){e.target.classList.add('in');io.unobserve(e.target);}
}),{threshold:.15,rootMargin:'0px 0px -8% 0px'});
document.querySelectorAll('.reveal').forEach(el=>io.observe(el));
```

## HEADER

- `position:fixed;top:0;` FULL WIDTH, `backdrop-filter:blur(12px)`,
  `background:rgba(18,18,18,.55)`, `border-bottom:1px solid var(--hair-5)`,
  HEIGHT ~72PX, `z-index:100`.
- 3-COLUMN GRID: LEFT NAV (`THE CELLAR / FLAVORS / MEMBERSHIP / JOURNAL`),
  CENTER ABSOLUTE SERIF LOGO `TERROIR` (LETTER-SPACING `.35em`, ~22PX),
  RIGHT `ACCOUNT` + BAG ICON. BAG HAS A SIENNA DOT (`8px`) THAT FADES IN ON HOVER.

## EDITORIAL HERO

- H1 `clamp(48px,8vw,96px)`, `line-height:.9`, SERIF. SECOND LINE ITALIC,
  `margin-left:24px`, SAGE-TINTED.
- BELOW: 3-COLUMN GRID, `border-top:1px solid var(--hair-10)` ON EACH CELL,
  PADDING-TOP `2rem`. COL 2/3 INCLUDE A `lucide:arrow-down` SVG WITH A SUBTLE
  BOUNCE: `@keyframes bounce-y{0%,100%{transform:translateY(0)}50%{transform:translateY(8px)}}`
  ~2S INFINITE EASE-IN-OUT.

## INTERACTIVE FLAVOR WALL — THE SIGNATURE GRID CARD

4-COLUMN GRID (`repeat(4,1fr)`, COLLAPSES TO 2 THEN 1), EACH CARD `height:600px`,
`position:relative;overflow:hidden`. THREE LAYERS:

```css
.card{position:relative;height:600px;overflow:hidden;cursor:pointer;}
.card .img{position:absolute;inset:0;width:100%;height:100%;object-fit:cover;
  opacity:0;transform:scale(1.05);mix-blend-mode:overlay;
  transition:opacity .7s ease,transform .7s ease;}
.card:hover .img{opacity:.40;transform:scale(1);}
.card .body{position:relative;z-index:2;height:100%;display:flex;
  flex-direction:column;justify-content:space-between;padding:2rem;}
.card .body>*{transition:transform .5s ease;}      /* slide-in-from-left 8px */
.card:hover .body>*{transform:translateX(8px);}
```
EACH CARD: `No. 01` LABEL TOP, FLAVOR NAME (SERIF, ~30PX) + TASTING NOTES AT
BOTTOM. BASE COLORS CYCLE `--card-a..d`.

## PRODUCT SPOTLIGHT

- `background:var(--panel)` (#262626), 50/50 GRID.
- LEFT: `aspect-ratio:3/4` IMAGE + `STAFF PICK` BADGE (`position:absolute`,
  `background:rgba(0,0,0,.4)`, `backdrop-filter:blur(8px)`, `border:1px solid rgba(255,255,255,.4)`,
  LABEL TYPE).
- RIGHT: PRODUCT NAME (SERIF), PRICE, A 2X2 SPEC GRID (TASTING NOTES / METHOD /
  RITUAL / VINTAGE) WITH HAIR-LINE BORDERS, AND A SOLID CREAM CTA: `background:var(--cream)`,
  `color:#121212`, LABEL TYPE.

## CASE BUILDER UTILITY + PROGRESS CAPACITY INDICATOR

- HEADER ROW: TITLE + THE CAPACITY INDICATOR (`128px×4px` TRACK, `--hair-10`
  BACKGROUND; INNER FILL SAGE, WIDTH = `count/6*100%`, `transition:width .4s ease`)
  WITH A MONO `4/6` READOUT.
- 3-COLUMN CARD GRID, EACH `background:var(--hair-5)`, `128px` GRAYSCALE BOTTLE
  PNG (`filter:grayscale(1)`, COLORS UP ON HOVER), AND CIRCULAR `−`/`+` BUTTONS
  (`32px`, `border:1px solid var(--hair-20)`, ROUND). QTY IS CLAMPED SO THE CASE
  NEVER EXCEEDS 6 BOTTLES; THE BAR + READOUT UPDATE LIVE.

```js
const CAP=6; let total=0;
function sync(){ total=[...qtys.values()].reduce((a,b)=>a+b,0);
  bar.style.width=Math.min(total/CAP*100,100)+'%';
  read.textContent=total+'/'+CAP; }
plus.onclick=()=>{ if(total<CAP){ set(id,get(id)+1); sync(); } };
minus.onclick=()=>{ if(get(id)>0){ set(id,get(id)-1); sync(); } };
```

## MEMBERSHIP COMPARISON

- TWO-COLUMN TABLE WITH A SINGLE CENTER `border-left:1px solid var(--hair-10)`.
- LEFT `GUEST` COLUMN `opacity:.6`; RIGHT `MEMBER` COLUMN FULL OPACITY WITH SIENNA
  TEXT ACCENTS ON THE HIGHLIGHTED PERKS. `JOIN THE CLUB` BUTTON IS OUTLINE-ONLY
  (`border:1px solid var(--hair-20)`, TRANSPARENT BG, HOVER FILLS CREAM).

## HOW IT'S MADE GRID

- 4-COLUMN PROCESS GRID. NUMERIC `01..04` IN SAGE (SERIF). GRAYSCALE PHOTO
  (`filter:grayscale(1)`) → COLOR ON HOVER OVER `700ms`. 14PX MANROPE COPY.

## FOOTER

- 4 COLUMNS: (1) SERIF `TERROIR` + ADDRESS, (2)(3) LINK LISTS, (4) NEWSLETTER
  WITH A BOTTOM-BORDER-ONLY INPUT (`border-bottom:1px solid var(--hair-20)`) AND
  A SIENNA `SUBSCRIBE →` TEXT LINK. BOTTOM BAR: 10PX COPYRIGHT + ALL-CAPS SOCIAL
  LINKS, SEPARATED BY A HAIR-LINE.

## RESPONSIVE

- ≥1024PX: FULL 4/3/2 GRIDS. 768–1023PX: FLAVOR WALL 2-COL, MADE GRID 2-COL,
  SPOTLIGHT STACKS. <768PX: EVERYTHING SINGLE COLUMN; HEADER NAV COLLAPSES TO
  LOGO + BAG; HERO H1 SCALES VIA `clamp`.
