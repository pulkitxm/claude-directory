# SUPER DESIGN — WARM INDUSTRIAL DESIGN SYSTEM

> NOTE: THE LIVE PREVIEW LINK SUPPLIED WITH THIS PROMPT WAS NETWORK-BLOCKED FROM
> THE BUILD ENVIRONMENT (HOST NOT IN EGRESS ALLOWLIST — WEBFETCH 403, CURL
> BLOCKED). THE BUILD WAS THEREFORE RECONSTRUCTED FROM THE FULL DESIGN SPEC
> BELOW (VERBATIM FROM PULKIT) PLUS THE IMPLEMENTATION DETAILS APPENDED AT THE
> END SO THAT RE-RUNNING THIS PROMPT REPRODUCES THE SAME UI.

---

## VERBATIM PROMPT (FROM PULKIT)

# SUMMARY

AN INDUSTRIAL-CHIC DESIGN SYSTEM CENTERED ON STRUCTURAL GRIDS, WARM GRAY TONES, AND HIGH-IMPACT EDITORIAL TYPOGRAPHY WITH TECHNICAL 'READOUT' ACCENTS.

# STYLE

THE STYLE IS DEFINED BY ITS 'WARM INDUSTRIAL' PALETTE (#EBEBE8 BACKGROUND) AND TECHNICAL PRECISION. IT USES 'INTER' FOR STRUCTURAL UI AND 'PLAYFAIR DISPLAY' FOR EDITORIAL FLAIR. ANIMATION IS CHARACTERIZED BY SMOOTH, HIGH-INERTIA TRANSITIONS (CUBIC-BEZIER(0.16, 1, 0.3, 1)) AND CLIP-PATH REVEALS. A CONSTANT 4% OPACITY FRACTAL NOISE OVERLAY PROVIDES A TACTILE, ANALOG FEEL TO THE DIGITAL INTERFACE.

## SPEC

APPLY A 'WARM INDUSTRIAL' AESTHETIC. COLORS: BACKGROUND #EBEBE8, FOREGROUND #18181B, ACCENT BLUE #0066FF, BORDERS #D4D4D8 (ZINC-300). TYPOGRAPHY: PRIMARY FONT 'INTER' (WEIGHTS 400, 700, 900), SECONDARY FONT 'PLAYFAIR DISPLAY' (ITALIC, WEIGHT 400). USE 10PX BOLD UPPERCASE TRACKING (0.2EM - 0.3EM) FOR ALL LABELS AND SMALL UI ELEMENTS. HERO HEADINGS SHOULD BE 8XL TO 10XL (128PX-160PX) WITH -0.05EM TRACKING. IMPLEMENT A GLOBAL NOISE TEXTURE OVERLAY USING A FRACTAL NOISE SVG AT 0.04 OPACITY. BORDERS SHOULD BE 1PX SOLID ZINC-300. USE A 12-COLUMN STRUCTURAL GRID MARKED BY THIN VERTICAL LINES ACROSS THE ENTIRE PAGE HEIGHT. INTERACTIVE ELEMENTS SHOULD USE A CUSTOM CUBIC-BEZIER(0.16, 1, 0.3, 1) TIMING FUNCTION FOR ALL TRANSFORMS AND CLIP-PATHS.

# LAYOUT & STRUCTURE

THE LAYOUT IS BUILT ON A RIGID 12-COLUMN GRID SYSTEM, WHERE SECTIONS ARE DIVIDED BY EXPLICIT 1PX HORIZONTAL AND VERTICAL LINES. IT FOLLOWS A VERTICAL STACK OF HIGH-IMPACT SECTIONS: NAVIGATION -> SPLIT HERO -> MARQUEE -> INTERACTIVE PROJECT LIST -> PHILOSOPHICAL PARALLAX -> TECHNICAL JOURNAL -> MASSIVE FOOTER.

## STRUCTURAL GRID

CREATE A FIXED, FULL-PAGE BACKGROUND LAYER CONSISTING OF 12 VERTICAL GRID LINES (#18181B AT 0.1 OPACITY) EVENLY SPACED ACROSS A MAX-WIDTH OF 1600PX, CENTERED. THESE LINES SHOULD PERSIST BEHIND ALL CONTENT.

## STICKY HEADER

HEIGHT: 80PX. BACKGROUND: #EBEBE8 AT 0.8 OPACITY WITH 10PX BACKDROP-BLUR. 1PX BOTTOM BORDER. LAYOUT: LEFT-ALIGNED NAV LINKS (TINY CAPS), ABSOLUTE CENTERED LOGO (SERIF ITALIC 'SUPER' + BOLD SANS 'DESIGN'), RIGHT-ALIGNED CTA BUTTON (ROUNDED-FULL, 1PX BORDER, 10PX CAPS).

## HERO SECTION

SPLIT 12-COLUMN LAYOUT. LEFT (7 COLS): VERTICAL STACK WITH 'AVAILABLE' STATUS INDICATOR, MASSIVE 3-LINE HEADING USING STROKE-TEXT AND SERIF ITALICS, TECHNICAL READOUT LABELS (10PX MONO), AND A LARGE RECTANGULAR 'START' BUTTON WITH A HOVER-ROTATING ARROW ICON. RIGHT (5 COLS): FULL-HEIGHT GRAYSCALE IMAGE WITH A 20PX INTERNAL FRAME AND A GLASSMORPHISM UI CARD OVERLAY CONTAINING 'SYSTEM STATUS' BARS AND MONO IDENTIFIERS.

## MARQUEE TICKER

FULL-WIDTH RIBBON, HEIGHT 120PX, BG #F4F4F5. CONTENT: RAPIDLY SCROLLING TEXT IN 7XL FONT, ALTERNATING BETWEEN HEAVY SANS-SERIF STROKE-TEXT AND ITALIC SERIF TEXT, SEPARATED BY ELECTRIC BLUE (#0066FF) STAR ICONS.

## PROJECT LIST

VERTICAL STACK OF ROWS. EACH ROW: 300PX HEIGHT, 1PX BORDER-BOTTOM. ON HOVER, THE TEXT SHIFTS TO ITALIC AND A LARGE GRAYSCALE IMAGE IS REVEALED FROM THE RIGHT USING A CLIP-PATH ANIMATION (INSET 0 0 0 100% TO 0 0 0 0). A FLOATING 'VIEW' CIRCLE WITH AN ARROW FOLLOWS THE CURSOR OR APPEARS ON THE RIGHT.

## FOOTER

BACKGROUND #18181B, TEXT #EBEBE8. LARGE-SCALE CONTACT LINK (4XL) WITH A BOTTOM-BORDER HOVER EFFECT. BOTTOM CONTAINS A MASSIVE 'BACKGROUND MARQUEE' AT 0.1 OPACITY WITH TEXT SIZED AT 20VW. FINAL ROW INCLUDES A GREEN 'SYSTEM OPERATIONAL' PULSE INDICATOR AND COPYRIGHT TEXT IN 10PX TRACKING.

# SPECIAL COMPONENTS

## STROKE TEXT EFFECT

HIGH-IMPACT DISPLAY TEXT THAT USES THIN OUTLINES INSTEAD OF SOLID FILLS.

IMPLEMENT TEXT WITH `-WEBKIT-TEXT-STROKE: 1PX #18181B; COLOR: TRANSPARENT;`. THIS SHOULD BE USED FOR MASSIVE HEADINGS TO CREATE VISUAL LIGHTNESS DESPITE LARGE SCALE.

## CLIP-PATH PROJECT REVEAL

AN IMAGE REVEAL INTERACTION FOR LIST ITEMS.

PROJECT IMAGES SHOULD BE HIDDEN BY DEFAULT USING `CLIP-PATH: INSET(0 0 0 100%)`. ON PARENT ROW HOVER, TRANSITION TO `CLIP-PATH: INSET(0 0 0 0)` OVER 0.6S USING CUBIC-BEZIER(0.16, 1, 0.3, 1). ADD A GRAYSCALE(1) FILTER TO THE IMAGE WITH A SUBTLE #0066FF/20 MIX-BLEND-MULTIPLY OVERLAY.

## STATUS PULSE

A TECHNICAL 'LIVE' INDICATOR.

CREATE A 6PX CIRCLE WITH BACKGROUND #22C55E (GREEN-500) AND AN OUTER PULSE ANIMATION USING `BOX-SHADOW` OR `SCALE` TO INDICATE REAL-TIME AVAILABILITY OR SYSTEM STATUS.

# SPECIAL NOTES

MUST: MAINTAIN THE #EBEBE8 BACKGROUND COLOR THROUGHOUT TO PRESERVE THE 'WARM INDUSTRIAL' FEEL. MUST: USE STRICT GRID ALIGNMENT—TEXT AND BOXES SHOULD ALIGN PERFECTLY WITH THE 12-COLUMN BACKGROUND LINES. DO NOT: USE ROUNDED CORNERS ON ANYTHING EXCEPT THE MAIN CTA BUTTONS AND SMALL STATUS BADGES; KEEP THE LAYOUT SHARP AND RECTANGULAR. MUST: KEEP THE NOISE TEXTURE VISIBLE BUT SUBTLE TO AVOID MAKING THE UI LOOK 'DIRTY'.

---

## IMPLEMENTATION DETAILS (TO REPRODUCE THE SAME UI)

### TECH / FILE SHAPE

- SINGLE SELF-CONTAINED `index.html` (NO BUILD STEP). ALL CSS IN A `<style>` BLOCK,
  ALL JS IN A `<script>` BLOCK AT END OF BODY. FULLY OFFLINE.
- FONTS VENDORED LOCALLY UNDER `assets/fonts/` (INTER 400/700/900, PLAYFAIR
  DISPLAY ITALIC 400) AND DECLARED WITH `@font-face` + `font-display:swap`.
  THE GRID/READOUT MONO LABELS USE A SYSTEM MONO STACK
  (`'SF Mono', ui-monospace, 'Roboto Mono', monospace`).
- HERO + PROJECT IMAGES VENDORED LOCALLY UNDER `assets/img/` AS GRAYSCALE
  PHOTOGRAPHY (ARCHITECTURE / INDUSTRIAL / STRUCTURE SUBJECTS). REFERENCED VIA
  RELATIVE PATHS.

### CSS TOKENS (`:root`)

```css
--bg:#EBEBE8; --fg:#18181B; --accent:#0066FF; --border:#D4D4D8;
--zinc100:#F4F4F5; --green:#22C55E;
--ease:cubic-bezier(0.16,1,0.3,1);
--maxw:1600px;
--label: 10px/1 'Inter', sans-serif; /* used as bold uppercase, tracking .25em */
```

GLOBAL: `*{box-sizing:border-box}` , `html{scroll-behavior:smooth}` ,
`body{background:var(--bg);color:var(--fg);font-family:'Inter';margin:0;
overflow-x:hidden}` . A REUSABLE `.label` CLASS:
`font:700 10px/1 'Inter';text-transform:uppercase;letter-spacing:.25em`.
A `.stroke` CLASS: `-webkit-text-stroke:1px var(--fg);color:transparent`.
A `.serif` CLASS: `font-family:'Playfair Display';font-style:italic;font-weight:400`.

### GLOBAL OVERLAYS (BOTH `position:fixed; inset:0; pointer-events:none`)

1. STRUCTURAL GRID (`z-index:1`): A CENTERED `max-width:1600px` CONTAINER HOLDING
   13 VERTICAL HAIRLINES VIA `repeating-linear-gradient` OR 13 ABSOLUTELY-PLACED
   1PX `div`S AT `left: calc(100%/12 * i)`, COLOR `rgba(24,24,27,.1)`.
   PERSISTS BEHIND ALL CONTENT, FIXED ON SCROLL.
2. NOISE OVERLAY (`z-index:9999`, `opacity:.04`, `mix-blend-mode:multiply`):
   AN INLINE SVG DATA URI:
   `url("data:image/svg+xml,...<filter><feTurbulence type='fractalNoise'
   baseFrequency='0.8' numOctaves='4'/></filter><rect filter='url(#n)'/>")`
   STRETCHED OVER THE VIEWPORT.

CONTENT WRAPPER SITS AT `z-index:2`, CENTERED `max-width:1600px`, WITH 1PX LEFT
AND RIGHT BORDERS (`--border`) SO CONTENT IS BOXED BY THE OUTER GRID.

### HEADER (STICKY, 80PX)

- `position:sticky;top:0;height:80px;z-index:50;`
  `background:rgba(235,235,232,.8);backdrop-filter:blur(10px);`
  `border-bottom:1px solid var(--border)`.
- THREE-PART FLEX ROW INSIDE THE 1600 CONTAINER WITH 24PX SIDE PADDING:
  - LEFT: NAV LINKS `WORK · STUDIO · JOURNAL` AS `.label` LINKS, HOVER → `--accent`.
  - CENTER (ABSOLUTE, CENTERED): LOGO = `<span class="serif">Super</span>` +
    `<span style="font-weight:900">DESIGN</span>` (NO SPACE / TIGHT).
  - RIGHT: CTA `<a>` — `START PROJECT` — `border-radius:999px;border:1px solid
    var(--fg);padding:10px 20px;` `.label`; HOVER FILLS `--fg` BG, `--bg` TEXT,
    TRANSITION `.4s var(--ease)`.

### HERO (12-COL CSS GRID, `min-height: calc(100vh - 80px)`)

- `display:grid;grid-template-columns:repeat(12,1fr)`. BORDER-BOTTOM 1PX.
- LEFT PANEL spans cols 1–7 (`grid-column:1 / 8`), 48PX PADDING, FLEX COLUMN,
  `justify-content:space-between`:
  - TOP: `.label` ROW — A `STATUS PULSE` GREEN DOT + `AVAILABLE FOR Q3 2026`.
  - MIDDLE: 3-LINE HEADING, `font-size:clamp(64px,10vw,160px);line-height:.9;
    letter-spacing:-.05em;font-weight:900`:
    - LINE 1: `BUILDING` (solid `--fg`).
    - LINE 2: `<span class="serif">digital</span>` (PLAYFAIR ITALIC, FILLED).
    - LINE 3: `STRUCTURES` (USES `.stroke` — OUTLINE ONLY).
  - BOTTOM ROW: LEFT = TWO MONO READOUT BLOCKS
    (`EST. 2019 / SAN FRANCISCO` AND `[ DESIGN · ENGINEERING · BRAND ]`),
    RIGHT = LARGE RECTANGULAR `START` BUTTON: SQUARE-ISH, `--fg` BG, `--bg` TEXT,
    `.label`, CONTAINING AN `↗` ARROW THAT ROTATES 45°→0° (i.e. `rotate(45deg)`
    AT REST → `rotate(0)` ON HOVER) AND TRANSLATES, `.5s var(--ease)`.
- RIGHT PANEL spans cols 8–12 (`grid-column:8 / 13`), `border-left:1px solid
  var(--border)`, `padding:20px` (THE 20PX INTERNAL FRAME), HOLDS A FULL-HEIGHT
  GRAYSCALE IMG (`object-fit:cover;filter:grayscale(1);width/height:100%`).
  - GLASSMORPHISM CARD OVERLAY, BOTTOM-LEFT, ABSOLUTE:
    `background:rgba(235,235,232,.6);backdrop-filter:blur(12px);
    border:1px solid var(--border);padding:16px;width:240px`.
    CONTENT: `.label` `SYSTEM STATUS`, THEN 3 LABELED PROGRESS BARS
    (DESIGN 92% / BUILD 78% / SHIP 64%) — A TRACK `height:4px;background:var(--border)`
    WITH A `--fg` FILL ANIMATED ON LOAD `.8s var(--ease)`, AND A MONO IDENTIFIER
    LINE `ID: SD-0xA1F · LAT 37.77 · LON -122.41`.

### MARQUEE (FULL-BLEED, 120PX, BG `--zinc100`)

- BREAK OUT OF THE 1600 CONTAINER TO 100VW. `overflow:hidden`, BORDER TOP+BOTTOM.
- A FLEX TRACK (`will-change:transform`) ANIMATED `@keyframes marquee{to{transform:
  translateX(-50%)}}` `animation:marquee 18s linear infinite` (DUPLICATE THE
  CONTENT ONCE FOR SEAMLESS LOOP). PAUSE ON HOVER.
- EACH ITEM `font-size:clamp(40px,7vw,84px);font-weight:900;white-space:nowrap`,
  ALTERNATING `.stroke` SANS AND `.serif` ITALIC, SEPARATED BY AN INLINE BLUE
  STAR `✶` (`color:var(--accent)`). SEQUENCE e.g.:
  `STRUCTURE ✶ <i>clarity</i> ✶ SYSTEMS ✶ <i>craft</i> ✶ INDUSTRIAL ✶ <i>precision</i> ✶`.

### PROJECT LIST (VERTICAL ROWS, EACH 300PX, BORDER-BOTTOM 1PX)

FOUR ROWS. EACH ROW IS A `position:relative;overflow:hidden` ANCHOR, FLEX
SPACE-BETWEEN, 48PX PADDING, ALIGN CENTER. STRUCTURE PER ROW:

- LEADING INDEX `.label` `(01)`…`(04)`.
- BIG TITLE `font-size:clamp(40px,6vw,96px);font-weight:700;transition:.5s var(--ease)`.
  ON ROW HOVER → `font-style:italic` AND `color` STAYS `--fg` (TITLE NUDGES
  `translateX(20px)`).
- TRAILING `.label` META (CATEGORY + YEAR), e.g. `BRAND SYSTEM — 2025`.
- HIDDEN REVEAL IMAGE: ABSOLUTE, RIGHT-ANCHORED, ~420×260, `filter:grayscale(1)`,
  `clip-path:inset(0 0 0 100%)`, `transition:clip-path .6s var(--ease)`;
  ON ROW HOVER → `clip-path:inset(0 0 0 0)`. AN ACCENT OVERLAY DIV ON TOP:
  `background:rgba(0,102,255,.2);mix-blend-mode:multiply`.
- FLOATING `VIEW` CIRCLE: A `pointer-events:none` 80PX CIRCLE
  (`border-radius:999px;background:var(--fg);color:var(--bg)`) WITH `VIEW ↗`,
  HIDDEN (`scale(0)`) BY DEFAULT, `scale(1)` ON HOVER (`.4s var(--ease)`),
  AND ITS `transform` TRANSLATE FOLLOWS THE CURSOR VIA JS `mousemove` ON THE ROW
  (LERPED `left/top` TO CURSOR POSITION).
- ROW HOVER ALSO TINTS BG `--zinc100`.

### PHILOSOPHICAL PARALLAX SECTION

- FULL-WIDTH, `--bg`, ~90VH, BORDER-BOTTOM. A CENTERED EDITORIAL STATEMENT:
  LARGE MIXED SANS/SERIF PARAGRAPH `font-size:clamp(28px,4vw,64px);
  line-height:1.1;max-width:1100px`, e.g.
  `We treat <span class="serif">design</span> as <span class="stroke">infrastructure</span>
  — load-bearing, measured, and built to <span class="serif">last</span>.`
- PARALLAX: ON SCROLL, A BACKGROUND `.label` WATERMARK AND/OR THE STATEMENT
  TRANSLATE AT DIFFERENT RATES VIA `window.scroll` → `translateY` (rAF-throttled).
  A SMALL MONO READOUT TOP-LEFT `( PHILOSOPHY / 04 )`.

### TECHNICAL JOURNAL SECTION

- HEADER ROW: `.label` `JOURNAL` LEFT, `.label` `VIEW ALL ↗` RIGHT, 1PX BORDER-BOTTOM.
- THREE-COLUMN GRID (`repeat(3,1fr)`, COLLAPSES TO 1 COL UNDER 900PX) OF JOURNAL
  ENTRIES; COLUMNS SEPARATED BY 1PX `--border`. EACH ENTRY: MONO DATE + INDEX,
  A `font-weight:700` 24–28PX TITLE (HOVER → ITALIC + `--accent`), A SHORT MONO
  EXCERPT, AND A `READ ↗` `.label`. ENTRIES e.g.
  `06.12.26 — ON STRUCTURAL HONESTY`, `05.28.26 — THE GRID AS GRAMMAR`,
  `05.02.26 — NOISE, SIGNAL, TEXTURE`.

### FOOTER (MASSIVE, BG `--fg`, TEXT `--bg`)

- `position:relative;overflow:hidden`. TOP: HUGE CONTACT LINK
  `<a>` `HELLO@SUPER.DESIGN` AT `font-size:clamp(32px,6vw,80px)` (4XL+),
  `border-bottom:2px solid transparent`, HOVER → BORDER `--bg` AND SLIGHT
  LETTER-SPACING SHIFT, `.4s var(--ease)`.
- COLUMN LINKS (SITEMAP / SOCIAL / LEGAL) AS `.label` STACKS.
- BACKGROUND MARQUEE: ABSOLUTE BOTTOM, `font-size:20vw;font-weight:900;
  white-space:nowrap;opacity:.1;` SCROLLING `SUPER DESIGN SUPER DESIGN`
  VIA THE SAME `marquee` KEYFRAME (SLOWER, e.g. 30s), SITS BEHIND THE BOTTOM ROW.
- FINAL ROW (ABOVE THE WATERMARK, 1PX BORDER-TOP `rgba(235,235,232,.2)`):
  LEFT = STATUS PULSE GREEN DOT + `.label` `SYSTEM OPERATIONAL`;
  RIGHT = `.label` `© 2026 SUPER DESIGN — ALL RIGHTS RESERVED`.

### ANIMATIONS — COMPLETE LIST (ALL USE `--ease` UNLESS NOTED)

1. CTA / START-PROJECT BUTTON: BG + TEXT COLOR INVERT ON HOVER, `.4s`.
2. START BUTTON ARROW: `rotate(45deg)→rotate(0)` + TRANSLATE ON HOVER, `.5s`.
3. HERO STATUS PULSE + FOOTER PULSE: `@keyframes pulse` SCALING A `box-shadow`
   RING `0 0 0 0 rgba(34,197,94,.6) → 0 0 0 8px rgba(34,197,94,0)`, `2s` LOOP.
4. HERO CARD PROGRESS BARS: WIDTH `0→target%` ON LOAD, `.8s`.
5. MARQUEE TICKER: `translateX(0→-50%)` LINEAR INFINITE `18s`; PAUSE ON HOVER.
6. PROJECT ROW TITLE: ROMAN→ITALIC + `translateX(20px)` ON HOVER, `.5s`.
7. PROJECT CLIP-PATH REVEAL: `inset(0 0 0 100%)→inset(0 0 0 0)` ON HOVER, `.6s`.
8. PROJECT VIEW CIRCLE: `scale(0)→scale(1)` ON HOVER `.4s`, POSITION LERPS TO
   CURSOR ON `mousemove`.
9. PROJECT ROW BG: TINT TO `--zinc100` ON HOVER, `.4s`.
10. PARALLAX: STATEMENT + WATERMARK `translateY` AT DIFFERENT RATES ON SCROLL
    (rAF-throttled scroll handler).
11. JOURNAL TITLE: ROMAN→ITALIC + `--accent` ON HOVER, `.3s`.
12. FOOTER CONTACT LINK: BOTTOM-BORDER REVEAL + LETTER-SPACING, `.4s`.
13. FOOTER BACKGROUND MARQUEE: `translateX` LINEAR INFINITE `30s` AT `.1` OPACITY.
14. ON-LOAD ENTRANCE: HERO HEADING LINES + LABELS FADE/SLIDE UP (`opacity 0→1`,
    `translateY(20px)→0`) WITH STAGGERED DELAYS VIA AN `IntersectionObserver`
    `.reveal` UTILITY (ALSO REUSED FOR SECTIONS AS THEY ENTER THE VIEWPORT).

### HARD CONSTRAINTS (REITERATED)

- `--bg` `#EBEBE8` EVERYWHERE EXCEPT MARQUEE (`#F4F4F5`) AND FOOTER (`#18181B`).
- NO ROUNDED CORNERS EXCEPT: CTA BUTTONS, STATUS BADGES, AND THE VIEW CIRCLE.
- NOISE OVERLAY VISIBLE BUT SUBTLE (`opacity:.04`).
- STRICT 12-COL ALIGNMENT; SECTIONS SEPARATED BY EXPLICIT 1PX LINES.
