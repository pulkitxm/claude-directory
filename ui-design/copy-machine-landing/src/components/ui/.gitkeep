# UI components go here.
# Per the project spec, all UI components live in /src/components/ui/.
# Each component the user provides will be added to this folder verbatim,
# then imported and stacked into /src/App.tsx.
