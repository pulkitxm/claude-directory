# UI Desgin

10 **UI Desgin** experiments generated with Claude Fable 5 — part of the [claude-directory](../README.md).

| Project | Description | Stack |
|---------|-------------|-------|
| [copy-machine-landing](./copy-machine-landing/) | Exact-spec single-page landing-page scaffold built to act as a "copy machine" — a Vite + React + TypeScript base with the `@/` path alias, Tailwind v3 (`@tailwind` base/components/utilities), a `cn()` clsx/tailwind-merge util, and framer-motion + lucide-react preinstalled, ready for components to be dropped verbatim into `/src/components/ui/` and stacked in `App.tsx` | React, TypeScript, Vite, Tailwind CSS, Framer Motion, clsx, tailwind-merge, Lucide |
| [copy-machine-landing-exact-spec](./copy-machine-landing-exact-spec/) | Setup-phase scaffold for a multi-turn "copy machine" landing-page brief — a single-page project stood up to an exact spec (Vite + React + TS, classic Tailwind directives, tailwind-merge + clsx `cn()`, Framer Motion, Lucide, `@/`→`/src/` alias), with character-for-character spec files and an empty `App.tsx` ready for components, verified to build/run headlessly | React, TypeScript, Vite, Tailwind CSS, tailwind-merge, clsx, Framer Motion, Lucide |
| [copy-machine-scaffold](./copy-machine-scaffold/) | Exact-spec "copy machine" landing-page scaffold built to a multi-turn setup prompt — Vite + React + TypeScript with the verbatim `@/`→`./src` alias (vite + tsconfig), Tailwind CSS + tailwind-merge/clsx `cn()` helper, Framer Motion and Lucide React, and the mandated `/src/components/ui` + `/src/lib` layout with an intentionally empty `App.tsx` shell; ships a type-checked production build and a CLI-only verifier (`scripts/verify.mjs`) that bundles against the real Vite config to assert the alias resolves, `cn()` merges Tailwind classes, `App.tsx` renders the exact spec'd markup, and framer-motion + lucide-react render | React, TypeScript, Vite, Tailwind CSS, tailwind-merge, clsx, Framer Motion, Lucide |
| [copy-machine-setup](./copy-machine-setup/) | "Copy Machine" landing-page scaffold built byte-for-byte to a fixed setup spec (Vite + React, TypeScript, Tailwind + tailwind-merge + clsx, Framer Motion, Lucide, `@/`→`/src/` alias) with the spec-pinned files reproduced character-for-character and an empty `App.tsx` awaiting the first component; ships a self-contained dark blueprint/IDE "PROJECT SETUP COMPLETE" status screen (radial-masked grid, drifting glow orbs, scanline, count-up readout, created-file tree, verbatim App.tsx code panel, tech-stack chips, copy-machine rules) rendered as a sibling of `#root`, with vendored Inter/JetBrains Mono, fully offline | React, TypeScript, Vite, Tailwind CSS, tailwind-merge, clsx, Framer Motion, Lucide |
| [industrial-skeuomorphism](./industrial-skeuomorphism/) | "SCHEMATIC" Industrial Skeuomorphism design-system showcase — a tactile hardware-control landing built on centralized neumorphic tokens, with an all-CSS hero device mockup (scanline screen + live dashboard), glowing LED status indicators, corner screws & vent slots on every panel, a cylindrical How-It-Works pipe connector, push-pinned testimonials with masking tape, punched-hole pricing tags, a conic-gradient radar sweep, recessed "data slot" inputs, and a matte-plastic noise overlay; vendored Inter/JetBrains Mono and all assets, fully offline & responsive | React, TypeScript, Vite, Tailwind CSS v4, Motion, Lucide |
| [landing-page-component-scaffold](./landing-page-component-scaffold/) | Exact-spec single-page landing-page scaffold set up before any components — Vite + React + TypeScript, Tailwind CSS with a clsx + tailwind-merge `cn()` helper, the `@/`→`src/` path alias wired in both vite.config and tsconfig, an empty `src/components/ui/` for stacked components, and a headless Chromium smoke test | React, TypeScript, Vite, Tailwind CSS, Framer Motion, Lucide, clsx, tailwind-merge |
| [landing-page-copy-machine](./landing-page-copy-machine/) | Exact-spec Vite + React + TypeScript landing-page scaffold for a strict "copy machine" workflow — `@/`→`/src/` alias wired in both `vite.config.ts` and `tsconfig.json`, a `cn()` (`twMerge(clsx())`) utility, Tailwind base styles, a `src/components/ui` drop zone, and an empty `relative min-h-screen flex flex-col` `App.tsx` stacking shell; deps (Framer Motion, Lucide, clsx, tailwind-merge) installed, builds clean, fully self-contained | React, TypeScript, Vite, Tailwind CSS, tailwind-merge, clsx, Framer Motion, Lucide |
| [landing-page-foundation-kit](./landing-page-foundation-kit/) | Single-page landing-page foundation/scaffold set up to an exact spec — Vite + React + TypeScript, Tailwind + tailwind-merge + clsx (`cn()` helper), Framer Motion and Lucide React, a `@/` → `/src/` path alias, and the fixed `/src/components/ui` + `/src/lib/utils.ts` + `App.tsx` structure, ready for components to be stacked in verbatim | React, TypeScript, Vite, Tailwind CSS, tailwind-merge, clsx, Framer Motion, Lucide |
| [landing-page-scaffold](./landing-page-scaffold/) | Pre-component landing-page scaffold built to an exact multi-turn spec — Vite + React + TypeScript, Tailwind CSS with tailwind-merge/clsx (`cn()` helper), Framer Motion and Lucide React installed, a verbatim `@/`→`/src/` path alias in vite/tsconfig, and the mandated `/src/components/ui` + `/src/lib` folder layout with an empty `App.tsx` shell ready for components to stack into | React, TypeScript, Vite, Tailwind CSS, tailwind-merge, clsx, Framer Motion, Lucide |
| [serif-editorial-design-system](./serif-editorial-design-system/) | "Roman & Quill" Serif design-system showcase — a private-publishing-house editorial landing (hero, stats, drop-cap features, asymmetric 1.3fr/0.7fr benefits, decorative-quote testimonials, gold-tinted featured pricing, journal grid, ARIA FAQ, inverted correspond form, footer) with vendored Playfair/Source Sans 3/IBM Plex Mono, a rule-line + small-caps label system, bespoke SVG engraving plates, burnished-gold accent, paper-grain overlay, and ambient glow — all on channel-format design tokens, fully offline | React, TypeScript, Vite, Tailwind CSS, Lucide |
